---
description: start work by checking out main and pulling the latest changes
---

// turbo-all

1. Checkout the `main` branch.
```bash
git checkout main
```

2. Pull the latest changes from the remote repository.
```bash
git pull origin main
```
